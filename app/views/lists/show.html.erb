<%- model_class = List -%>
<div class="page-header">
  <h1><%= t("list.show", name: @list.name) %></h1>
</div>

<table class="table table-striped table-condensed table-hover span4">
  <thead>
  	<th><%= t('user.full_name')%></th>
  	<th><%= t('user.email')%></th>
  	<th></th>
  </thead>
  <tbody>
  	
  	<% @subscriptions.each do |s| %>
  	  <tr>
  	    <td><%= link_to s.user.full_name, user_path(s.user) %></td>
  	    <td><%= link_to s.user.email, user_path(s.user) %></td>
  	    <td>
  	    	<%= link_to t('list.remove_user'), subscription_path(s), method: :delete, class: 'btn btn-mini btn-danger' %>
  	    </td>
  	  </tr>
  	<%end %>
  </tbody>
</table>
<dl class="dl-horizontal">
  	
</dl>

<div class="form-actions">
  <%= link_to t("helpers.links.back"),
              lists_path, :class => 'btn'  %>
  <%= link_to t("helpers.links.edit"),
              edit_list_path(@list), :class => 'btn' %>
  <%= link_to t("helpers.links.destroy"),
              list_path(@list),
              :method => 'delete',
              :data => { :confirm => t("helpers.links.confirm", :default => 'Are you sure?') },
              :class => 'btn btn-danger' %>
</div>
